# This directory will control all the custom hooks needed for the application.

# At this moment, there is no need for custom hooks, but we will need a lot of custom hooks to scale up the application.
